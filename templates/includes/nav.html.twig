

<nav class="navbar navbar-expand-lg navbar-light">
    <div class="d-flex flex-grow-1">
        <span class="w-100 d-lg-none d-block"><!-- hidden spacer to center brand on mobile --></span>
        <a class="navbar-brand d-none d-lg-inline-block" href="{{ path('home') }}">
            <img id="logo" src="{{ asset('img/logo_HW.png') }}" alt="logo">
            <small>la plateforme qui organise vos sorties !</small>
        </a>
        <a class="navbar-brand-two mx-auto d-lg-none d-inline-block" href="#">
            <img src="{{ asset('img/logo_HW.png') }}" alt="logo">
        </a>
        <div class="w-100 text-right">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#myNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
    </div>
    <div class="collapse navbar-collapse flex-grow-1 text-right" id="myNavbar">
        <ul class="navbar-nav ml-auto flex-nowrap">
            <li class="nav-item">
                <a class="nav-link m-2 menu-item nav-active" href="{{ path('home') }}">Accueil</a>
            </li>
            {% if app.user %}
            <li class="nav-item">
                <a class="nav-link m-2 menu-item" href="{{ path('profilModif') }}">Mon Profil</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a href="#" class="nav-link m-2 menu-item">Villes</a>
            </li>
            <li class="nav-item">
                <a href="#" class="nav-link m-2 menu-item">Sites</a>
            </li>
                <li class="nav-item">
                    <a class="nav-link m-2 menu-item" href="{{ path('register') }}">Inscription</a>
                </li>
            {% endif %}
            {% if app.user %}
            <li class="nav-item">
                <a class="nav-link m-2 menu-item" href="{{ path('logout') }}">Se déconnecter</a>
            </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link m-2 menu-item" href="{{ path('login') }}">Se connecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
